/bin/
/ondaire
/data/
data*/
*.test
coverage.out

# Svelte/Vite: deps and build output; keep the committed placeholder for go:embed.
web/node_modules/
web/dist/*
!web/dist/index.html

*.wav
*.flac
*.mp3

# Audio test fixtures are real committed files despite the format ignores above.
!internal/audio/testdata/*
respot
# Open Design local workspace (runtime data + secrets)
design/open_design_data/
design/.env
