stages: [frontend, test, build, release]

workflow:
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/   # release pipeline (vX.Y.Z)
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never                                  # no duplicate branch+MR pipelines
    - if: $CI_COMMIT_BRANCH

variables:
  CGO_ENABLED: "0"
  GOMODCACHE: "$CI_PROJECT_DIR/.gomodcache"

# 1) Build the embedded UI; pass web/dist to later stages as an artifact.
frontend:
  stage: frontend
  image: node:24-alpine
  cache:
    key: { files: [web/package-lock.json] }
    paths: [web/node_modules]
  script:
    - cd web
    - npm ci
    - npm run build          # emits web/dist
  artifacts:
    paths: [web/dist]
    expire_in: 1 day
  tags:
    - shared

# 2) Vet + unit tests + gofmt + an arm64 cross-build smoke (pure Go, no toolchain).
test:
  stage: test
  image: golang:1.26
  needs: [frontend]
  cache:
    key: gomod
    paths: [.gomodcache]
  script:
    - go vet ./...
    - go test ./...
    - out="$(gofmt -l cmd internal *.go | grep -vE "node_modules|\.gomodcache" || true)"; if [ -n "$out" ]; then echo "gofmt:"; echo "$out"; exit 1; fi
    - GOOS=linux GOARCH=arm64 go build ./...   # proves the arm64 cross-build stays clean
  tags:
    - shared

# 2b) Loopback end-to-end smoke: two nodes, gossip via --join (no multicast),
# null sinks, the full scripts/e2e.sh. Opus/alsa dlopen probes simply report
# the capability off inside the container; e2e's capability leg reads the
# container's own ldconfig, so it stays truthful either way.
e2e:
  stage: test
  image: golang:1.26
  needs: [frontend]
  # Integration smoke over a real multi-node loopback cluster. Timing-sensitive
  # in shared CI runners (slow scheduling can starve the playout deadline), so
  # it never runs automatically — trigger it by hand from the pipeline when
  # wanted. The unit `test` job is the release gate.
  rules:
    - when: manual
      allow_failure: true
  cache: { key: gomod, paths: [.gomodcache], policy: pull }
  variables:
    DEBIAN_FRONTEND: noninteractive
  script:
    - apt-get update -qq && apt-get install -y -qq --no-install-recommends jq python3 libopus0 libasound2 > /dev/null
    - ./scripts/e2e.sh
  tags:
    - shared

# 3) Cross-compile every target in parallel → downloadable artifacts.
# Branch/MR commits build 4-week dev artifacts (grab from the job page) and
# publish NO release. Tag pipelines use build-release below (never expires).
.build-template:
  stage: build
  image: golang:1.26
  needs: [frontend, test]
  cache: { key: gomod, paths: [.gomodcache], policy: pull }
  parallel:
    matrix:
      - { GOOS: linux, GOARCH: amd64,  ARCH: amd64 }
      - { GOOS: linux, GOARCH: arm64,  ARCH: arm64 }
      # 32-bit ARM (armv6/armhf, Pi Zero / Pi 1) is no longer supported — the
      # soft-/hard-float loader split was never reliable enough. 64-bit only: amd64
      # + arm64 (Raspberry Pi OS 64-bit on a Pi 3/4/5/Zero 2).
  script:
    - VER="${CI_COMMIT_TAG:-$CI_COMMIT_SHORT_SHA}"   # release tag, else dev sha
    - SUFFIX="${GOOS}-${ARCH}"
    - LDFLAGS="-s -w -X main.version=$VER"
    - if [ -n "${INTERP:-}" ]; then LDFLAGS="$LDFLAGS -I $INTERP"; fi
    - go build -trimpath -ldflags "$LDFLAGS"
        -o "bin/ondaire-$SUFFIX" ./cmd/ondaire
    # Hand this job's id to the release stage (dotenv): release asset links
    # must address the EXACT build job — the by-ref artifacts endpoint resolves
    # branch/tag names only, never a commit SHA.
    - ARCH_KEY=$(echo "$ARCH" | tr '[:lower:]' '[:upper:]')
    - echo "BUILD_JOB_ID_${ARCH_KEY}=$CI_JOB_ID" > build.env
  tags:
    - shared

build:
  extends: .build-template
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - when: on_success
  artifacts:
    name: "ondaire-${CI_COMMIT_REF_SLUG}"
    paths: [bin/]
    reports:
      dotenv: build.env
    expire_in: 4 weeks

build-release:
  extends: .build-template
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
  artifacts:
    name: "ondaire-${CI_COMMIT_TAG}"
    paths: [bin/]
    reports:
      dotenv: build.env
    expire_in: never

# 3b) ESP32 playback-node firmware. Built in the official ESP-IDF image (it has
# the toolchain + cmake/ninja/patch) — one matrix job per board class, each
# producing a single merged flash image (fw/ondaire-fw-<board>.bin) the web
# flasher serves. Branch/MR pipelines build only when esp32/** changed; tag
# pipelines always build (firmware-release, never-expiring artifacts for the
# release links + the site image). See esp32/ and docs/developer/esp32.md.
.firmware-template:
  stage: build
  image: espressif/idf:release-v5.4
  needs: []
  # ESP32 firmware is bring-up, not shipped yet: never block the pipeline or the
  # release on it. Remove allow_failure once the firmware is release-ready.
  allow_failure: true
  # BOARD is the board profile (esp32/boards/ + sdkconfig.defaults.<board>); TARGET
  # is the IDF silicon (set-target). They differ when boards share a chip — the
  # Super Mini is esp32s3 silicon with its own PSRAM-on config. CHIP names the
  # dotenv var for the release asset links.
  parallel:
    matrix:
      - { BOARD: esp32s3-supermini, TARGET: esp32s3, CHIP: ESP32S3 }
      - { BOARD: esp32s3-zero, TARGET: esp32s3, CHIP: ESP32S3ZERO }
      - { BOARD: esp32s3-amped-plus, TARGET: esp32s3, CHIP: ESP32S3AMPEDPLUS }
      - { BOARD: esp32s3-hifi, TARGET: esp32s3, CHIP: ESP32S3HIFI }
      - { BOARD: esp32s3-amped, TARGET: esp32s3, CHIP: ESP32S3AMPED }
      - { BOARD: esp32s3-loud, TARGET: esp32s3, CHIP: ESP32S3LOUD }
      - { BOARD: esp32s3-hifi-plus, TARGET: esp32s3, CHIP: ESP32S3HIFIPLUS }
      - { BOARD: esp32s3-louder, TARGET: esp32s3, CHIP: ESP32S3LOUDER }
      - { BOARD: esp32s3-louder-plus, TARGET: esp32s3, CHIP: ESP32S3LOUDERPLUS }
      - { BOARD: esp32s3-loud-plus, TARGET: esp32s3, CHIP: ESP32S3LOUDPLUS }
  # Use the IDF + tools PRE-INSTALLED in the espressif/idf image. (This previously
  # overrode IDF_TOOLS_PATH to a project-local .espressif and cached it — but on a
  # cache miss that dir is empty, so export.sh cannot find the Python venv and the
  # build fails with "virtual environment ... not found". The image already has
  # everything under $IDF_PATH/$IDF_TOOLS_PATH; just source export.sh.)
  script:
    - . "$IDF_PATH/export.sh"
    - cd esp32
    # Per-board sdkconfig in the build dir so the matrix jobs never share config.
    - IDFARGS="-B build-$BOARD -DBOARD=$BOARD -DSDKCONFIG=build-$BOARD/sdkconfig"
    - idf.py $IDFARGS set-target "$TARGET"
    - idf.py $IDFARGS build
    - idf.py $IDFARGS merge-bin -o "ondaire-fw-$BOARD.bin"   # → build-$BOARD/
    - mkdir -p ../fw
    # Merged image (flash-all, offset 0) + the app-only image (keep-config, the
    # ota_0 app at 0x20000) the flasher writes for a config-preserving update.
    - cp "build-$BOARD/ondaire-fw-$BOARD.bin" "../fw/ondaire-fw-$BOARD.bin"
    - cp "build-$BOARD/ondaire-node.bin" "../fw/ondaire-app-$BOARD.bin"
    - cd ..
    # Per-board job id for the release asset links (dotenv; by-ref artifacts
    # endpoint resolves names only, never a SHA — same reason as the Go build).
    - echo "FW_JOB_ID_${CHIP}=$CI_JOB_ID" > firmware.env
  tags:
    - shared

firmware:
  extends: .firmware-template
  rules:
    - if: $CI_COMMIT_TAG
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes: [esp32/**/*]
    - if: $CI_COMMIT_BRANCH
      changes: [esp32/**/*]
  artifacts:
    name: "ondaire-fw-${CI_COMMIT_REF_SLUG}"
    paths: [fw/]
    reports:
      dotenv: firmware.env
    expire_in: 4 weeks

firmware-release:
  extends: .firmware-template
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
  artifacts:
    name: "ondaire-fw-${CI_COMMIT_TAG}"
    paths: [fw/]
    reports:
      dotenv: firmware.env
    expire_in: never

# 3c) Multi-arch (amd64 + arm64) Docker image of an ondaire MASTER, Spotify built
# in. REUSES the per-arch binaries from the build stage (already version-stamped +
# SPA-embedded) rather than recompiling — the Dockerfile just drops the right one
# onto the go-librespot base. Pushes :dev on the default branch, and :<tag>/:latest
# on a release tag. Needs a registry-auth file in the CONFIG_JSON file variable.
docker:
  stage: build
  image: docker:28-cli
  # Reuse the build job's bin/ artifacts (build on branches, build-release on
  # tags) — whichever ran in this pipeline; both publish the same bin/ paths.
  needs:
    - job: build
      optional: true
      artifacts: true
    - job: build-release
      optional: true
      artifacts: true
  # No dind service / DOCKER_HOST: the runner bind-mounts the host's
  # /var/run/docker.sock, so the docker CLI talks straight to the host daemon
  # over the default unix socket. binfmt + a docker-container buildx builder give
  # the multi-platform manifest.
  variables:
    IMAGE: "harbor.rand0m.me/public/ondaire"
    PLATFORMS: "linux/amd64,linux/arm64"
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  before_script:
    - mkdir -p ~/.docker
    - cp "${CONFIG_JSON}" ~/.docker/config.json
    # Register QEMU so the arm64 runtime-stage apk add runs under emulation.
    - docker run --privileged --rm tonistiigi/binfmt --install all
    # Fresh docker-container builder (the default "docker" driver can't emit a
    # multi-platform manifest). Idempotent across the shared host daemon.
    - docker buildx rm ondaire-builder >/dev/null 2>&1 || true
    - docker buildx create --use --name ondaire-builder
  script:
    # main → "dev"; release tag → the tag itself (also moves :latest). The binary
    # carries its own -X main.version stamp from the build job, so no build-arg.
    - VER="${CI_COMMIT_TAG:-dev}"
    - TAGS="-t $IMAGE:$VER"; if [ -n "$CI_COMMIT_TAG" ]; then TAGS="$TAGS -t $IMAGE:latest"; fi
    - docker buildx build
      --platform "$PLATFORMS"
      -f docker/Dockerfile
      $TAGS
      --push
      .
  after_script:
    - docker buildx rm ondaire-builder >/dev/null 2>&1 || true
  tags:
    - shared

# 3d) Multi-arch image of the marketing site (site/) served by nginx. Self-
# contained: the generator runs in the build stage, only the static output ships.
# Pushes :dev on the default branch, :<tag>/:latest on a release tag.
docker-site:
  stage: build
  image: docker:28-cli
  # Reuse the per-arch binaries from the build stage (download page) plus the
  # ESP32 firmware images (flasher page); all optional so the site still builds
  # when a given producer job didn't run this pipeline.
  variables:
    IMAGE: "harbor.rand0m.me/public/ondaire-site"
    PLATFORMS: "linux/amd64,linux/arm64"
  needs:
    - job: build
      optional: true
      artifacts: true
    - job: build-release
      optional: true
      artifacts: true
    - job: firmware
      optional: true
      artifacts: true
    - job: firmware-release
      optional: true
      artifacts: true
    - job: homeassistant
      optional: true
      artifacts: true
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  before_script:
    - mkdir -p ~/.docker
    - cp "${CONFIG_JSON}" ~/.docker/config.json
    - docker run --privileged --rm tonistiigi/binfmt --install all
    - docker buildx rm site-builder >/dev/null 2>&1 || true
    - docker buildx create --name site-builder
  script:
    - VER="${CI_COMMIT_TAG:-dev}"
    - TAGS="-t $IMAGE:$VER"; if [ -n "$CI_COMMIT_TAG" ]; then TAGS="$TAGS -t $IMAGE:latest"; fi
    # Stage the prebuilt binaries as tar.gz (renamed to `ondaire` inside) where
    # build.mjs hashes them into the download page, and the installer for /get.sh.
    - |
      mkdir -p site/src/assets/downloads
      for bin in bin/ondaire-linux-*; do
        name=$(basename "$bin")
        tmp=$(mktemp -d)
        cp "$bin" "$tmp/ondaire"
        tar -czf "site/src/assets/downloads/$name.tar.gz" -C "$tmp" ondaire
        rm -rf "$tmp"
      done
      cp scripts/get.sh site/get.sh
    # Stage the Home Assistant integration zip (from the homeassistant job) for
    # the download page; absent on non-tag pipelines → the card shows "not staged".
    - cp ondaire-hacs.zip site/src/assets/downloads/ 2>/dev/null || true
    # Stage the ESP32 firmware images where build.mjs hashes them into the
    # flasher page + ESP Web Tools manifest (absent if no firmware job ran).
    - |
      mkdir -p site/src/assets/firmware
      cp fw/ondaire-fw-*.bin site/src/assets/firmware/ 2>/dev/null || true
      cp fw/ondaire-app-*.bin site/src/assets/firmware/ 2>/dev/null || true
    - docker buildx build --builder site-builder
      --platform "$PLATFORMS"
      --build-arg VER="$VER"
      -f site/Dockerfile
      $TAGS
      --push
      site
  after_script:
    - docker buildx rm site-builder >/dev/null 2>&1 || true
  tags:
    - shared

# 3e) Home Assistant custom integration — zip custom_components/ondaire for
# HACS / manual install. Tags only; never-expiring artifact (grab it from the
# release / job page and unzip into <config>). No build step: it's pure Python.
homeassistant:
  stage: build
  image: alpine:latest
  needs: []
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
  script:
    - apk add --no-cache zip
    - ( cd integrations/homeassistant && zip -r "$CI_PROJECT_DIR/ondaire-hacs.zip" custom_components/ondaire hacs.json README.md -x '*/__pycache__/*' )
    # Hand this job's id to the release stage (dotenv) so the Release asset link
    # addresses the EXACT job — the by-ref artifacts endpoint resolves refs, not SHAs.
    - echo "HACS_JOB_ID=$CI_JOB_ID" > hacs.env
  artifacts:
    name: "ondaire-hacs-${CI_COMMIT_TAG}"
    paths: [ondaire-hacs.zip]
    expire_in: never
    reports:
      dotenv: hacs.env
  tags:
    - shared

# The Android companion app lives in its own repo (share/ondaire-android) and
# builds/releases its APK there.

# 4) Changelog for a tagged release: commits since the previous v* tag.
changelog:
  stage: build
  image: golang:1.26   # any image with full git
  needs: []
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
  variables:
    GIT_DEPTH: "0"     # full history so the previous tag is reachable
  script:
    - PREV=$(git tag --list 'v[0-9]*' --sort=-v:refname | grep -vx "$CI_COMMIT_TAG" | head -1 || true)
    - |
      {
        echo "## $CI_COMMIT_TAG"
        echo
        if [ -n "$PREV" ]; then
          echo "Changes since $PREV:"; echo
          git log --no-merges --pretty='- %s' "$PREV..$CI_COMMIT_TAG"
        else
          echo "Initial release."; echo
          git log --no-merges --pretty='- %s' "$CI_COMMIT_TAG" | head -50
        fi
        echo
        echo "Pure-Go binaries for linux/amd64 + linux/arm64 (64-bit only; use Raspberry Pi OS 64-bit on a Pi 3/4/5/Zero 2)."
        echo "Optional libraries (libopus, libasound) are loaded at runtime."
        echo "Android companion APK: see the share/ondaire-android project."
      } > release-notes.md
    - cat release-notes.md
  artifacts:
    paths: [release-notes.md]
    expire_in: 1 week
  tags:
    - shared

# 5) Publish a GitLab Release — TAGS ONLY (vX.Y.Z). The release process:
#
#     git tag -a v0.2.0 -m "v0.2.0" && git push origin v0.2.0
#
# That single push triggers this pipeline: full test + e2e gate, both
# binaries built with -X main.version=v0.2.0, a changelog generated from the
# commits since the previous tag, and a GitLab Release whose asset links
# address the EXACT build jobs by id (dotenv from the build matrix — the
# by-ref artifacts endpoint resolves branch/tag names only, never a SHA).
# Tagged-build artifacts never expire. Ordinary branch commits still build
# 4-week dev artifacts (grab them from the job page) but publish no release.
release:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  # Firmware is NOT a release dependency yet (allow_failure, bring-up): the release
  # ships the linux binaries only, so a failing esp32 build never blocks shipping.
  # Re-add firmware-release to needs + an asset link when the firmware is ready.
  needs: [build-release, changelog, test, homeassistant]
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
  script: [ 'echo "Releasing $CI_COMMIT_TAG (amd64 $BUILD_JOB_ID_AMD64, arm64 $BUILD_JOB_ID_ARM64, hacs $HACS_JOB_ID)"' ]
  release:
    tag_name: "$CI_COMMIT_TAG"
    name: "Ondaire $CI_COMMIT_TAG"
    description: "./release-notes.md"
    assets:
      links:
        - name: "ondaire-linux-amd64 ($CI_COMMIT_TAG)"
          url: "$CI_PROJECT_URL/-/jobs/$BUILD_JOB_ID_AMD64/artifacts/raw/bin/ondaire-linux-amd64"
        - name: "ondaire-linux-arm64 ($CI_COMMIT_TAG)"
          url: "$CI_PROJECT_URL/-/jobs/$BUILD_JOB_ID_ARM64/artifacts/raw/bin/ondaire-linux-arm64"
        - name: "Home Assistant integration ($CI_COMMIT_TAG)"
          url: "$CI_PROJECT_URL/-/jobs/$HACS_JOB_ID/artifacts/raw/ondaire-hacs.zip"
          link_type: package
  tags:
    - shared

# 6) Mirror the release to GitHub (github.com/rmalchow/ondaire) — TAGS ONLY.
# Runs only after the full test/build/release stages succeed (no `needs`, so it
# waits on the prior stages), so a broken tag never reaches GitHub. Pushes the
# tag's commit to GitHub `main` and the tag itself; histories already match, so
# this fast-forwards. Requires a CI/CD variable GITHUB_TOKEN holding a GitHub PAT
# with Contents: read/write on the repo (fine-grained PAT, or a classic token
# with `repo` scope). Mark it Masked.
github-mirror:
  stage: release
  image: alpine:latest
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+/
  variables:
    GIT_DEPTH: "0"   # full history so the fast-forward push has every ancestor
    GITHUB_REPO: "github.com/rmalchow/ondaire.git"
  script:
    - apk add --no-cache git
    - git remote remove github 2>/dev/null || true
    - git remote add github "https://x-access-token:${GITHUB_TOKEN}@${GITHUB_REPO}"
    - git push github "HEAD:refs/heads/main"   # advance GitHub main to the tagged commit
    - git push github "refs/tags/$CI_COMMIT_TAG"
  tags:
    - shared
