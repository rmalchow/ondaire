// Package clock is the NTP-style 4-timestamp UDP offset estimator and the
// ClockSource. The master runs the server; the follower runs the estimator.
// Leaf package: imports no sibling internal packages.
package clock
