// Package discovery does LAN broadcast / mDNS announce and browse, emitting
// bootstrap seed addresses. Bootstrap only, never the membership source of
// truth. May import config only.
package discovery
