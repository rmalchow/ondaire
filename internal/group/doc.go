// Package group is the group engine: it holds the node's current role, drives
// the master/follower lifecycle, owns the per-group Timeline, and runs profile
// negotiation. The orchestrator below cmd; not imported by web.
package group
