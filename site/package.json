{
  "name": "ondaire-site",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing site for ondaire — a zero-dependency static generator.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "node build.mjs && cd dist && python3 -m http.server 8000"
  }
}
